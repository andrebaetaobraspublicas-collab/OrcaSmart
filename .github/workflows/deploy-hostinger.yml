name: Deploy Hostinger

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Bundle Node runtime dependencies
        run: |
          npm install --prefix .deploy-runtime --omit=dev --ignore-scripts mysql2@3.11.5 pdf-parse@1.1.1 xlsx@0.18.5
          mkdir -p node_modules
          cp -R .deploy-runtime/node_modules/. node_modules/
          rm -rf .deploy-runtime

      - name: Signal Node restart
        run: |
          mkdir -p tmp
          date -u +"%Y-%m-%dT%H:%M:%SZ" > tmp/restart.txt

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTINGER_FTP_SERVER }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          local-dir: ./
          server-dir: ${{ secrets.HOSTINGER_TARGET_DIR }}
          exclude: |
            **/.git*
            **/.git*/**
            **/__pycache__/**
            **/*.pyc
            **/.env
            **/*.db
            **/*.sqlite
            **/*.sqlite3
            **/tenant_dbs/**
            **/exports/**
